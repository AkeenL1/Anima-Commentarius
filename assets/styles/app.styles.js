import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        display: "flex",
        flex: 1,
        justifyContent: "space-between",
        backgroundColor: '#c2af88',
    },
    loginButton: {
        bottom: 200,
    }
});

export default styles